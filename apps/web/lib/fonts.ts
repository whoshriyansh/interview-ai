import { Lato, Inter } from "next/font/google";

export const geistLato = Lato({
  weight: ["400", "700"],
  subsets: ["latin"],
});

export const geistInter = Inter({
  weight: ["400", "700"],
  subsets: ["latin"],
});
